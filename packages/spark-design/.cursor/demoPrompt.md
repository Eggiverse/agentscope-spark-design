<analysis_phase>
获取 masterGo dsl 后，必须按照以下步骤处理，禁止跳过任何步骤
【第零步：使用指定的rule】
- 使用 `.cursor/rules/mastergo.mdc` rule
- 使用 `.cursor/rules/标准组件列表.mdc` rule

【第一步：数据分析】
1. 按照 `节点树分析规范` 分析节点结构

【第二步：组件分类】
1. 根据 `节点遍历规则` 进行组件分类，输出格式
| 组件名 | 组件分类 | 实现组件 |
| xxx | 标准组件 | ❌ |
| xxx | 标准组件的子组件 | ❌ |
| xxx | 自定义组件 | ✅ |

【第三步：查看组件文档】
1. 【强制】查看并严格遵循 `.cursor/rules/标准组件列表.mdc` 中的 `样式设置规则` 和 所有标准组件的 `组件使用规则`
2. 【强制】查看所有标准组件文档
  - 先列出所有标准组件
  - 使用 `list_dir` 工具查看 `.cursor/agentscope-ai-design-md` 目录下的文档列表
  - 【强制】必须使用 `read_file` 工具读取每个标准组件的文档内容
  - 若找不到文档，则使用 `antd` 组件
3. 【强制】输出信息
| 组件名 | 样式设置规则 | 组件使用规则 |
| xxx | xxx | xxx |
| xxx | xxx | - |
| xxx | - | - |

【第四步：样式处理】
1. 布局尽量使用 antd 的 `Flex` 组件
2. 禁止设置任何样式

【第五步：代码实现】
1. 实现所有自定义组件
2. 使用自定义组件和标准组件实现UI
4. 【强制】检查所有`标准组件`，删除和默认值相同的props
  - 不要自作主张，一定要删除

【第六步：验证检查】
1. 输出说明:
  - 列出所有做出的关键决策
  - 说明任何需要特别注意的地方
  - 标注任何需要确认的问题

</analysis_phase>
